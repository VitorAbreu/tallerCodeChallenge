import { Routes } from '@angular/router';
import { MainFetchComponent } from './components/main-fetch/main-fetch.component';

export const routes: Routes = [
  {
    path: '',
    component: MainFetchComponent,
  },
];
